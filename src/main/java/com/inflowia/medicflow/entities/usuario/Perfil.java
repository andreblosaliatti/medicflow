package com.inflowia.medicflow.entities.usuario;

//*Enum que define os perfis de acesso ao sistema Medicflow. */

public enum Perfil {
    ADMIN,
    MEDICO,
    SECRETARIO,
    ATENDENTE,
    ENFERMEIRO,
    PACIENTE

}
