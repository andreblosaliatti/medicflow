# Flyway baseline reset workspace

Esta pasta foi limpa para preparar a nova baseline estrutural.
As migrations históricas foram movidas para `db/legacy-migration`.

Próximo passo esperado: criar uma nova `V1__baseline_schema.sql` alinhada ao modelo de domínio refatorado.
