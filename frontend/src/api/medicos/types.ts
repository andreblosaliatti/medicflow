export type MedicoSelectApi = {
  id: number;
  nomeCompleto: string;
  crm: string | null;
  especialidade: string | null;
};

export type MedicoOptionViewModel = {
  id: number;
  label: string;
};
